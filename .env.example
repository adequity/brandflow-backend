# BrandFlow 환경 변수 템플릿
# 실제 배포시 .env 파일로 복사하여 실제 값으로 변경하세요

# ================================
# 기본 애플리케이션 설정
# ================================
ENVIRONMENT=production
SECRET_KEY=your-super-secret-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ================================
# 데이터베이스 설정 - Railway PostgreSQL
# ================================
# Railway PostgreSQL (기본 설정)
DATABASE_URL=postgresql+asyncpg://postgres:kAPUkGlWqoHwxIvtWaeukQuwcrZpSzuu@junction.proxy.rlwy.net:21652/railway

# 로컬 PostgreSQL (선택사항)
# DATABASE_URL=postgresql://brandflow_user:your_password@postgres:5432/brandflow
POSTGRES_USER=brandflow_user
POSTGRES_PASSWORD=brandflow_password_2024
POSTGRES_DB=brandflow

# ================================
# Redis 설정 (캐싱 & 세션)
# ================================
REDIS_URL=redis://:brandflow-redis-pass@redis:6379/0
REDIS_PASSWORD=brandflow-redis-pass

# ================================
# CORS 설정
# ================================
# 프론트엔드 URL들 (쉼표로 구분)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5175,https://yourdomain.com

# ================================
# 보안 설정
# ================================
# JWT 토큰 설정
JWT_SECRET_KEY=your-jwt-secret-key-here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Rate Limiting (분당 요청 수)
RATE_LIMIT_PER_MINUTE=100
RATE_LIMIT_PER_SECOND=10

# ================================
# 파일 업로드 설정
# ================================
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760  # 10MB in bytes
ALLOWED_FILE_EXTENSIONS=jpg,jpeg,png,gif,pdf,doc,docx,xls,xlsx

# ================================
# 이메일 설정 (알림용)
# ================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@yourdomain.com

# ================================
# 로깅 설정
# ================================
LOG_LEVEL=INFO
LOG_FILE_PATH=./logs/app.log
LOG_MAX_SIZE=10485760  # 10MB
LOG_BACKUP_COUNT=5

# ================================
# 성능 모니터링
# ================================
PERFORMANCE_MONITORING=true
SLOW_QUERY_THRESHOLD=1.0  # seconds
MEMORY_WARNING_THRESHOLD=85  # percentage

# ================================
# 보안 감사
# ================================
SECURITY_AUDIT_ENABLED=true
AUDIT_LOG_RETENTION_DAYS=90
VULNERABILITY_SCAN_ENABLED=true

# ================================
# SSL/TLS 설정 (Nginx용)
# ================================
SSL_CERT_PATH=/etc/ssl/certs/brandflow.crt
SSL_KEY_PATH=/etc/ssl/certs/brandflow.key

# ================================
# 백업 설정
# ================================
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *  # 매일 새벽 2시
BACKUP_RETENTION_DAYS=30
BACKUP_PATH=./backups

# ================================
# 외부 서비스 통합
# ================================
# AWS S3 (파일 스토리지)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=ap-northeast-2
S3_BUCKET=brandflow-uploads

# Sentry (에러 추적)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ================================
# 개발용 설정
# ================================
# 개발 환경에서만 사용
DEBUG=false
RELOAD=false
WORKERS=1

# 테스트 데이터베이스
TEST_DATABASE_URL=postgresql://test_user:test_password@localhost:5432/brandflow_test