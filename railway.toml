[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT --reload"
restartPolicyType = "ALWAYS"

[env]
PYTHONPATH = "."
VERSION = "2.3.0"
DEPLOY_FORCE = "true"
NIXPACKS_PYTHON_VERSION = "3.11.6"
RAILWAY_FORCE_REBUILD = "true"