# Database Configuration
DATABASE_URL=sqlite+aiosqlite:///./brandflow.db  # Development (SQLite)
# DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/brandflow  # Production (PostgreSQL)

# JWT Configuration
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Server Configuration
DEBUG=true
CORS_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000"]

# PostgreSQL Configuration (when using PostgreSQL)
POSTGRES_USER=brandflow_user
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=brandflow
POSTGRES_HOST=localhost
POSTGRES_PORT=5432